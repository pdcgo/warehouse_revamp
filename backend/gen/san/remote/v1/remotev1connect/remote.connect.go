// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: san/remote/v1/remote.proto

// san.remote.v1 is a TOOL contract, not a warehouse domain contract — which is why it sits
// under san/ rather than warehouse/. Nothing the warehouse serves belongs here, and nothing
// here is ever mounted on the application server: RemoteService runs a shell, and the one place
// that must never happen is the process serving customers.
package remotev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/pdcgo/warehouse_revamp/backend/gen/san/remote/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RemoteServiceName is the fully-qualified name of the RemoteService service.
	RemoteServiceName = "san.remote.v1.RemoteService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RemoteServiceInfoProcedure is the fully-qualified name of the RemoteService's Info RPC.
	RemoteServiceInfoProcedure = "/san.remote.v1.RemoteService/Info"
	// RemoteServiceExecProcedure is the fully-qualified name of the RemoteService's Exec RPC.
	RemoteServiceExecProcedure = "/san.remote.v1.RemoteService/Exec"
)

// RemoteServiceClient is a client for the san.remote.v1.RemoteService service.
type RemoteServiceClient interface {
	// Info is the handshake: where the workspace is, what shell will run the command, when the
	// token dies. An agent calls it once before doing anything, and it doubles as the cheapest
	// possible check that the token is good.
	Info(context.Context, *connect.Request[v1.InfoRequest]) (*connect.Response[v1.InfoResponse], error)
	// Exec runs ONE command and streams its output as it is produced.
	Exec(context.Context, *connect.Request[v1.ExecRequest]) (*connect.ServerStreamForClient[v1.ExecResponse], error)
}

// NewRemoteServiceClient constructs a client for the san.remote.v1.RemoteService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRemoteServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RemoteServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	remoteServiceMethods := v1.File_san_remote_v1_remote_proto.Services().ByName("RemoteService").Methods()
	return &remoteServiceClient{
		info: connect.NewClient[v1.InfoRequest, v1.InfoResponse](
			httpClient,
			baseURL+RemoteServiceInfoProcedure,
			connect.WithSchema(remoteServiceMethods.ByName("Info")),
			connect.WithClientOptions(opts...),
		),
		exec: connect.NewClient[v1.ExecRequest, v1.ExecResponse](
			httpClient,
			baseURL+RemoteServiceExecProcedure,
			connect.WithSchema(remoteServiceMethods.ByName("Exec")),
			connect.WithClientOptions(opts...),
		),
	}
}

// remoteServiceClient implements RemoteServiceClient.
type remoteServiceClient struct {
	info *connect.Client[v1.InfoRequest, v1.InfoResponse]
	exec *connect.Client[v1.ExecRequest, v1.ExecResponse]
}

// Info calls san.remote.v1.RemoteService.Info.
func (c *remoteServiceClient) Info(ctx context.Context, req *connect.Request[v1.InfoRequest]) (*connect.Response[v1.InfoResponse], error) {
	return c.info.CallUnary(ctx, req)
}

// Exec calls san.remote.v1.RemoteService.Exec.
func (c *remoteServiceClient) Exec(ctx context.Context, req *connect.Request[v1.ExecRequest]) (*connect.ServerStreamForClient[v1.ExecResponse], error) {
	return c.exec.CallServerStream(ctx, req)
}

// RemoteServiceHandler is an implementation of the san.remote.v1.RemoteService service.
type RemoteServiceHandler interface {
	// Info is the handshake: where the workspace is, what shell will run the command, when the
	// token dies. An agent calls it once before doing anything, and it doubles as the cheapest
	// possible check that the token is good.
	Info(context.Context, *connect.Request[v1.InfoRequest]) (*connect.Response[v1.InfoResponse], error)
	// Exec runs ONE command and streams its output as it is produced.
	Exec(context.Context, *connect.Request[v1.ExecRequest], *connect.ServerStream[v1.ExecResponse]) error
}

// NewRemoteServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRemoteServiceHandler(svc RemoteServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	remoteServiceMethods := v1.File_san_remote_v1_remote_proto.Services().ByName("RemoteService").Methods()
	remoteServiceInfoHandler := connect.NewUnaryHandler(
		RemoteServiceInfoProcedure,
		svc.Info,
		connect.WithSchema(remoteServiceMethods.ByName("Info")),
		connect.WithHandlerOptions(opts...),
	)
	remoteServiceExecHandler := connect.NewServerStreamHandler(
		RemoteServiceExecProcedure,
		svc.Exec,
		connect.WithSchema(remoteServiceMethods.ByName("Exec")),
		connect.WithHandlerOptions(opts...),
	)
	return "/san.remote.v1.RemoteService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RemoteServiceInfoProcedure:
			remoteServiceInfoHandler.ServeHTTP(w, r)
		case RemoteServiceExecProcedure:
			remoteServiceExecHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRemoteServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRemoteServiceHandler struct{}

func (UnimplementedRemoteServiceHandler) Info(context.Context, *connect.Request[v1.InfoRequest]) (*connect.Response[v1.InfoResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("san.remote.v1.RemoteService.Info is not implemented"))
}

func (UnimplementedRemoteServiceHandler) Exec(context.Context, *connect.Request[v1.ExecRequest], *connect.ServerStream[v1.ExecResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("san.remote.v1.RemoteService.Exec is not implemented"))
}
